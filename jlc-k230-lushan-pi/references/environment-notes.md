# Environment Notes

Use these notes as tested environment facts when the user has not provided a different current setup.

## Firmware Reference

Board-tested firmware reference:

```text
CanMV_K230_LCKFB_micropython_v1.6-57-gce3418e_nncase_v2.11.0
```

Treat this as a reference version for compatibility and debugging, not as a universal requirement. If API behavior differs, ask the user to confirm their board firmware string and compare it with the official wiki/API pages.

When this firmware changes, update this file and add a short note to `maintenance.md#revision-log`.
